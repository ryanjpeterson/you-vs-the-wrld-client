import React from "react";

import GridContainer from "../../components/grid-container/grid-container.component";

const HomePage = ({ posts }) => <GridContainer posts={posts} />;
export default HomePage;
